<UserControl x:Class="Tulpep.Integritul.Views.ResultOfComparisionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Name="MainGrid" Margin="0,0,0,0">
        <StackPanel>
            <TextBlock Opacity="0.7"  FontSize="16" TextAlignment="Justify" TextWrapping="Wrap" x:Name="ResultsTitle" Margin="50,40,50,0">
                Here are the results of your comparison per every file
            </TextBlock>
            <DataGrid ItemsSource="{Binding ResultList}" 
              CanUserReorderColumns="True" CanUserResizeColumns="True" 
              CanUserResizeRows="False" CanUserSortColumns="True"/>
        </StackPanel>
    </Grid>
</UserControl>
