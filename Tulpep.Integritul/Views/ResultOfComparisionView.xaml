<UserControl x:Class="Tulpep.Integritul.Views.ResultOfComparisionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Name="MainGrid" Margin="0,0,0,0">
        <StackPanel>
            <TextBlock Opacity="0.8"  FontSize="22" TextWrapping="Wrap" x:Name="ResultsTitle" Margin="50,40,50,40" Foreground="Black" HorizontalAlignment="Center" TextAlignment="Center">
                Here are the results of your comparison per every file in the folder
            </TextBlock>
            <Button x:Name="Home" Content="{StaticResource appbar_home_empty}" ToolTip="Home" Style="{DynamicResource MetroCircleButtonStyle}" Width="100" Height="100" Margin="0,0,0,0"/>
            <DataGrid ItemsSource="{Binding ResultList}" 
              CanUserReorderColumns="True" CanUserResizeColumns="True" 
              CanUserResizeRows="False" CanUserSortColumns="True" Margin="30,0,30,0"/>
        </StackPanel>
    </Grid>
</UserControl>
